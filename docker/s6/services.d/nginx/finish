#!/usr/bin/execlineb -S0
# S6 finish script for NGINX
# Called when nginx exits — logs the exit code

foreground { echo "[s6] nginx exited with code ${1}" }
ifelse { s6-test ${1} -eq 0 } { exit 0 }
echo "[s6] nginx crashed — S6 will restart it"
